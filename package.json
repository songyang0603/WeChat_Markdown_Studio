{
  "name": "wechat-markdown-studio",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.11.0",
  "description": "Mono-repo for Markdown to WeChat layout studio with themes, templates, and quality checks.",
  "scripts": {
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "dev:web": "pnpm --filter web-studio dev",
    "dev:api": "pnpm --filter api dev"
  },
  "devDependencies": {
    "@eslint/js": "^9.14.0",
    "@types/hast": "^3.0.4",
    "typescript": "^5.6.2",
    "typescript-eslint": "^8.10.0"
  },
  "dependencies": {
    "unist-util-visit": "^5.0.0"
  }
}
