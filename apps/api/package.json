{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "lint": "eslint --ext .ts src",
    "test": "vitest run --config ../../configs/vitest.config.ts"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "fastify": "^5.0.0",
    "zod": "^3.23.8",
    "@theme-engine/core": "workspace:*",
    "@markdown-pipeline/core": "workspace:*"
  },
  "devDependencies": {
    "eslint": "^9.11.1",
    "tsx": "^4.17.0",
    "typescript": "^5.6.2",
    "vitest": "^2.1.0"
  }
}
